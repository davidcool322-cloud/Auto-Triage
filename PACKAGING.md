# Hyper-RMA 打包發布指南

本專案支援將 Python 環境與外部工具 (External Tools) 打包為獨立的可執行檔 (Portable Executable)，以解決內網跨網段限制問題。

## 📦 打包方式

1. **執行自動化腳本**
   在專案根目錄執行以下指令：
   ```batch
   build_dist.bat
   ```
   *此腳本會自動安裝 `pyinstaller`，編譯程式碼，並將外部工具整合至輸出目錄。*

2. **取得執行檔**
   打包完成後，檔案位於：
   `dist/Hyper-RMA/`

3. **分發**
   將整個 `dist/Hyper-RMA` 資料夾壓縮 (ZIP/7z)，傳送給一般使用者即可。

## 🚀 使用者如何執行
使用者解壓縮後，進入資料夾雙擊：
**`Hyper-RMA.exe`**

---

## ⚠️ 注意事項
* **檔案路徑**：`build_dist.bat` 預設假設外部工具位於 `..\tools` (專案上層目錄)。若您的工具來源路徑不同，請修改腳本中的 `SAA_SOURCE` 變數。
* **首次啟動**：首次啟動可能會花費約 10-20 秒進行解壓縮與初始化，請耐心等待。
* **瀏覽器**：程式啟動後會自動開啟預設瀏覽器。關閉終端機視窗即會結束程式。
